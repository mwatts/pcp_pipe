[package]
name = "pcp-fetcher"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
tracing = { workspace = true }
reqwest = { version = "0.12", features = ["stream"] }
url = "2"
bytes = "1"
